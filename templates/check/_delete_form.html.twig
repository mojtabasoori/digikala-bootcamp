<form method="post" action="{{ path('app_check_delete', {'id': check.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ check.id) }}">
    <button class="btn">Delete</button>
</form>
